# Glassmorphism look'n'feel — blue/teal frosted glass

$glassBorder = rgb(6fb3e0)

general {
    # No gaps between windows or borders
     gaps_in = 2
     gaps_out = 2
     border_size = 2
     col.active_border = $glassBorder
}

# https://wiki.hypr.land/Configuring/Basics/Variables/#decoration
decoration {
    # Use round window corners
    rounding = 20
       blur {
        enabled = true
        size = 6
        passes = 3
        new_optimizations = true
        contrast = 1.0
        brightness = 1.05
        vibrancy = 0.18
        }
    shadow {
        enabled = true
        range = 22
        render_power = 3
        color = rgba(00000072)
    }
    # Dim unfocused windows (0.0 = no dim, 1.0 = fully dimmed)
    # dim_inactive = true
    # dim_strength = 0.15
}
# All windows: active 0.96 / inactive 0.90 — mostly opaque so app content
# shows its real (lighter) colors instead of blending down to near-black
windowrule = opacity 0.96 0.90, match:class .*

# Layer surfaces: blur only where the surface is tightly sized to its
# visible content (control-center, walker, swayosd). waybar and swaync's
# notification-window surfaces reserve a much bigger transparent bounding
# box than what's visually shown, so blurring them tints/blurs that whole
# empty box instead of just the pill/card — keep those CSS-glass only.
layerrule = blur off, match:namespace waybar
layerrule = blur on, match:namespace swaync-control-center
layerrule = blur off, match:namespace swaync-notification-window
layerrule = blur on, match:namespace walker
layerrule = blur on, match:namespace swayosd
# https://wiki.hypr.land/Configuring/Basics/Variables/#animations
animations {
     enabled = yes
    animation = windows,1,7,default
    animation = fade,1,7,default
    animation = workspaces,1,6,default
    animation = layers,1,3,default,slide

}

# https://wiki.hypr.land/Configuring/Basics/Variables/#layout
layout {
    # Avoid overly wide single-window layouts on wide screens
    # single_window_aspect_ratio = 1 1
}

# https://wiki.hypr.land/Configuring/Layouts/Scrolling-Layout/
scrolling {
    # See only one column per screen instead of two
    # column_width = 0.97
}

