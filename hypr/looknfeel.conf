# Glassmorphism look'n'feel — blue/teal frosted glass

$glassBorder = rgb(6fb3e0)

general {
    # No gaps between windows or borders
     gaps_in = 2
     gaps_out = 2
     border_size = 2
     col.active_border = $glassBorder
}

# https://wiki.hypr.land/Configuring/Basics/Variables/#decoration
decoration {
    # Use round window corners
    rounding = 20
       blur {
        enabled = true
        size = 6
        passes = 3
        new_optimizations = true
        contrast = 1.0
        brightness = 1.20
        vibrancy = 0.22
        }
    shadow {
        enabled = true
        range = 22
        render_power = 3
        color = rgba(00000072)
    }
    # Dim unfocused windows (0.0 = no dim, 1.0 = fully dimmed)
    # dim_inactive = true
    # dim_strength = 0.15
}
# All windows: active 0.78 / inactive 0.68 — see-through enough that the
# blurred (bright) backdrop lightens the app instead of a near-opaque dark panel
windowrule = opacity 0.78 0.68, match:class .*

# Layer surfaces: waybar, swaync (one shared ~500x846 surface for both toast
# popups and the control-center panel, confirmed via `hyprctl layers`), and
# swayosd all reserve more surface than their visible pill/card, and blur
# bleeds into that reserve as a rectangular halo around the rounded shape
# no matter how tightly the CSS content is sized — so compositor blur stays
# off for all of them; their glass look comes from CSS transparency alone.
# walker's surface is genuinely tightly fit, so blur is safe there.
layerrule = blur off, match:namespace waybar
layerrule = blur off, match:namespace swaync-notification-window
layerrule = blur off, match:namespace swayosd
layerrule = blur on, match:namespace walker
# https://wiki.hypr.land/Configuring/Basics/Variables/#animations
animations {
     enabled = yes
    animation = windows,1,7,default
    animation = fade,1,7,default
    animation = workspaces,1,6,default
    animation = layers,1,3,default,slide

}

# https://wiki.hypr.land/Configuring/Basics/Variables/#layout
layout {
    # Avoid overly wide single-window layouts on wide screens
    # single_window_aspect_ratio = 1 1
}

# https://wiki.hypr.land/Configuring/Layouts/Scrolling-Layout/
scrolling {
    # See only one column per screen instead of two
    # column_width = 0.97
}

